spring:
  application:
    name: demo
  datasource:
    url: jdbc:mysql://localhost:3306/SpringMysql
    username: lpq
    password: io
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update

